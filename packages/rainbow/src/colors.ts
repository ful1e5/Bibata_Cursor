import { ThemeColors } from "bibata-core/src/types";

// Colors
const none = "#00FF00";
const black = "#000000";

const themeColors: ThemeColors = {
  Rainbow: {
    base: none, // .svg base by default animated
    outline: black
  }
};

export { themeColors };
