import { ThemeColors } from "bibata-core/src/types";

// Colors
const black = "#000000";

const themeColors: ThemeColors = {
  Rainbow: {
    base: black, // .svg base by default animated
    outline: black
  }
};

export { themeColors };
