branches:
  only:
    - master
    - devlopment
language: minimal
sudo: required
services:
  - docker
before_install:
  - docker pull kaizkhatri/xinkpypi:v1
  - docker build --rm -f "Dockerfile" -t bibata_cursor:latest .
